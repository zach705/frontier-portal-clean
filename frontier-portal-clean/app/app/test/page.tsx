export default function TestPage() {
  return (
    <div style={{ padding: 24, fontFamily: 'system-ui' }}>
      <h1>Test route OK ✅</h1>
      <p>This confirms Vercel routing is working for this deployment.</p>
      <p>Now we can focus on the root <code>/</code> page.</p>
    </div>
  );
}
